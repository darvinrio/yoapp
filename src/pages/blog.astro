---
import PageTitle from "../components/PageTitle.astro";
import Layout from "../layouts/Layout.astro";
import { getCollection, type CollectionEntry } from "astro:content";

const posts: CollectionEntry<"blog">[] = (await getCollection("blog")).sort(
  (a: CollectionEntry<"blog">, b: CollectionEntry<"blog">) =>
    b.data.date.valueOf() - a.data.date.valueOf(),
);
---

<Layout
  title="Writings"
  description="Writings on on-chain data, DeFi analytics, and prediction markets by maybeYonas."
>
  <main class="container-max px-4 py-12 font-text">
    <PageTitle title="Writings" backHref="/" />
    <ul class="flex flex-col gap-6">
      {
        posts.map((post) => (
          <li class="border-b border-line pb-6 last:border-0">
            <a href={`/blog/${post.id}`} class="group block">
              <h2 class="font-text text-2xl group-hover:text-accent transition-colors">
                {post.data.title}
              </h2>
              <time
                datetime={post.data.date.toISOString()}
                class="text-sm text-ink-faint mt-1 block"
              >
                {post.data.date.toLocaleDateString("en-US", {
                  year: "numeric",
                  month: "long",
                  day: "numeric",
                })}
              </time>
              <p class="mt-2 text-ink-muted">{post.data.description}</p>
              <div class="mt-3 flex flex-wrap gap-2">
                {post.data.tags.map((tag: string) => (
                  <span class="text-xs px-2 py-0.5 rounded-full bg-accent-soft text-accent-soft-ink">
                    {tag}
                  </span>
                ))}
              </div>
            </a>
          </li>
        ))
      }
    </ul>
  </main>
</Layout>
